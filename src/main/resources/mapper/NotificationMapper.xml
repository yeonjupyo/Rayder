<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.likelion.backend.notification.mapper.NotificationMapper">

	<resultMap id="settingResultMap" type="com.likelion.backend.notification.domain.NotificationSetting">
		<id property="notificationId" column="noti_id"/>
		<result property="userId" column="user_id"/>
		<result property="type" column="noti_type" javaType="com.likelion.backend.notification.domain.NotificationType"/>
		<result property="enabled" column="is_active"/>
		<result property="createdAt" column="created_at"/>
		<result property="updatedAt" column="updated_at"/>
	</resultMap>

	<resultMap id="warningResultMap" type="com.likelion.backend.notification.domain.NotificationWarningSetting">
		<id property="userId" column="user_id"/>
		<result property="enabled" column="is_active"/>
		<result property="createdAt" column="created_at"/>
		<result property="updatedAt" column="updated_at"/>
	</resultMap>

	<select id="existsUser" resultType="boolean">
		SELECT EXISTS(SELECT 1 FROM `USER` WHERE user_id = #{userId})
	</select>

	<select id="findAllByUserId" resultMap="settingResultMap">
		SELECT noti_id, user_id, noti_type, is_active, created_at, updated_at
		FROM NOTIFICATION_SETTING WHERE user_id = #{userId}
		ORDER BY FIELD(noti_type, 'UV', 'DUST', 'ROUTINE')
	</select>

	<select id="findById" resultMap="settingResultMap">
		SELECT noti_id, user_id, noti_type, is_active, created_at, updated_at
		FROM NOTIFICATION_SETTING WHERE noti_id = #{notificationId}
	</select>

	<select id="findByUserIdAndType" resultMap="settingResultMap">
		SELECT noti_id, user_id, noti_type, is_active, created_at, updated_at
		FROM NOTIFICATION_SETTING
		WHERE user_id = #{userId} AND noti_type = #{type}
	</select>

	<select id="findTimes" resultType="java.time.LocalTime">
		SELECT alert_time FROM NOTIFICATION_TIME
		WHERE noti_id = #{notificationId} ORDER BY alert_time
	</select>

	<insert id="insertSetting" parameterType="com.likelion.backend.notification.domain.NotificationSetting"
		useGeneratedKeys="true" keyProperty="notificationId" keyColumn="noti_id">
		INSERT INTO NOTIFICATION_SETTING (user_id, noti_type, is_active)
		VALUES (#{userId}, #{type}, #{enabled})
	</insert>

	<update id="updateSetting" parameterType="com.likelion.backend.notification.domain.NotificationSetting">
		UPDATE NOTIFICATION_SETTING SET is_active = #{enabled} WHERE noti_id = #{notificationId}
	</update>

	<delete id="deleteSetting">
		DELETE FROM NOTIFICATION_SETTING WHERE noti_id = #{notificationId}
	</delete>

	<delete id="deleteTimes">
		DELETE FROM NOTIFICATION_TIME WHERE noti_id = #{notificationId}
	</delete>

	<insert id="insertTimes">
		INSERT INTO NOTIFICATION_TIME (noti_id, alert_time) VALUES
		<foreach collection="times" item="time" separator=",">
			(#{notificationId}, #{time})
		</foreach>
	</insert>

	<select id="findWarningByUserId" resultMap="warningResultMap">
		SELECT user_id, is_active, created_at, updated_at
		FROM NOTIFICATION_WARNING_SETTING WHERE user_id = #{userId}
	</select>

	<insert id="upsertWarning">
		INSERT INTO NOTIFICATION_WARNING_SETTING (user_id, is_active)
		VALUES (#{userId}, #{enabled})
		ON DUPLICATE KEY UPDATE is_active = VALUES(is_active)
	</insert>
</mapper>
