<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.uvmate.mapper.ChatMapper">

    <select id="findConversationId" resultType="Integer">
        SELECT conversation_id
        FROM CHATBOT_CONVERSATION
        WHERE user_id = #{userId}
        ORDER BY started_at DESC
        LIMIT 1
    </select>

    <insert id="insertConversation" useGeneratedKeys="true" keyProperty="holder.conversationId">
        INSERT INTO CHATBOT_CONVERSATION (user_id)
        VALUES (#{userId})
    </insert>

    <insert id="insertMessage">
        INSERT INTO CHATBOT_MESSAGE (conversation_id, sender_type, message_content)
        VALUES (#{conversationId}, #{senderType}, #{messageContent})
    </insert>

</mapper>